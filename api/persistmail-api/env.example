# Environment variables for PersistMail API
# Copy this file to .env and fill in your values

# API Settings
PROJECT_NAME=PersistMail API
API_V1_STR=/api/v1

# CORS Settings (comma-separated list of allowed origins)
CORS_ORIGINS_STR=http://localhost:3000,http://localhost:8080,https://yourdomain.com

# Database Settings
DATABASE_URL=sqlite:///./persistmail.db

# Mailcow Settings (REQUIRED for Mailcow integration)
MAILCOW_API_URL=https://mail.yourdomain.com
MAILCOW_API_KEY=your-mailcow-api-key-here
MAILCOW_DOMAIN=yourdomain.com

# Legacy Mail Server Settings (for backward compatibility with non-Mailcow domains)
MAIL_DOMAIN=yourdomain.com
IMAP_HOST=imap.yourdomain.com
IMAP_PORT=993
SMTP_HOST=smtp.yourdomain.com
SMTP_PORT=465
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=your-admin-password
IMAP_SECRET=your-shared-imap-secret
IS_PREMIUM_DOMAIN=false

# Email Settings
DEFAULT_HOURS_RETENTION=24
DEFAULT_EMAIL_LIMIT=25
MAX_EMAIL_LIMIT=50

# Mailbox Settings
DEFAULT_MAILBOX_QUOTA=50
MAILBOX_EXPIRY_HOURS=24
PASSWORD_LENGTH=16

# Rate Limiting
RATE_LIMIT_PER_MINUTE=60
